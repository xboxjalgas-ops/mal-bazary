{
  "name": "mal-bazary",
  "private": true,
  "description": "Мал Базары — фермерлерге арналған онлайн-нарық",
  "scripts": {
    "test": "node --test",
    "check": "node --check js/app.js && node --test"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
